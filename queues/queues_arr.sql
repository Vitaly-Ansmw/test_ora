create table queues_arr (
                         id number,
                         collection_id number,
                         c_type number,
                         c_id_query number,
                         c_active number(1),
                         constraint z#PK#queues_arr primary key (id)
                         );
                         
                         

